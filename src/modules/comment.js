/* const [image, setImage] = useState({});
  let img = {};

  let Img = function (res) {
    let i = Object.keys(res).length;
    for (i; i > 0; i--) {
      img[i] = "<img src={image[" + i + "]}>";
    }
  };
  const Image = function () {
    fetch("/add", {
      method: "POST",
    })
      .then((response) => response.json())
      .then((response) => Img(response));
  };

  
  
    const Boo = function () {
    let i = Object.keys(image).length;
    for (i; i > 0; i--) console.log(image[i]);
  };

    let Img = function (res) {
    let i = Object.keys(res).length;
    for (i; i > 0; i--) {
      img[i] = "<img src={image[" + i + "]}>";
    }
  };



*/
